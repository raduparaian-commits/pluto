[project]
name = "pluto"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Radu", email = "raduparaian@icloud.com" }
]
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.3",
    "torch>=2.14.0",
]

[project.scripts]
pluto = "pluto:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
